import React from 'react';
import PublicHeader from './PublicHeader';

/**
 * Unified Header for Student and Public pages
 * Ensures 100% UI/UX consistency across all views
 */
export default function StudentHeader() {
  return <PublicHeader />;
}
